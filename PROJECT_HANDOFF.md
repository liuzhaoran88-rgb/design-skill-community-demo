# Dong Design 设计与用研提效社区｜项目交接说明

> 交接版本：2026-07-16  
> Demo 入口：`index.html`  
> 本地服务：`node server.js` → `http://127.0.0.1:8765/index.html`

## 1. 项目一句话

围绕设计与用研 AI 提效能力，把分散在个人、项目与 Agent 中的知识、Skill、工具和实践经验，转化为团队可发现、可复用、可共建、可持续演进的公共能力，并让每一次有效贡献被看见。

官网核心表达：

> 让经验被共享，让贡献被看见。

## 2. 大目标与判断标准

项目目标是完成三个跃迁：

```text
个人经验与工具 → 团队公共能力 → 可持续演进的组织 AI 能力
```

北极星目标不是“社区里有多少条内容”，而是：

> 有多少公共能力被真实使用、共同维护并持续迭代。

核心判断标准：

- 公共能力是否有清晰的问题、场景、调用方式和维护人。
- 设计师是否能在真实任务中快速发现并调用它。
- 使用后的问题、建议和案例是否能回流到下一轮迭代。
- 创建、共建、验证、反馈和维护是否可以被准确归因。

## 3. 产品架构：一个目标、三个重点、两个底座

### 3.1 公共能力沉淀

将分散产出转化为结构清晰、可被维护的公共能力。

当前重点资产：

- 16.0 设计系统：设计哲学、组件规范、场域规范。
- 公共 Skill：可被 Agent 调用的设计能力。
- Vibe Coding 小工具：解决具体问题的轻量工具。
- 独立应用 / 工作台：面向完整场景的垂类 AI 能力。

产品重点：统一资产身份、简介、场景、版本、贡献者、调用方式和关联内容。

### 3.2 能力发现与复用

让公共能力进入真实工作，而不是停留在仓库中。

社区承担：

- 汇总本周新增与变化。
- 持续呈现 16.0 设计规范建设进展。
- 推荐值得使用的 Skill、工具和独立应用。
- 展示商详大脑等标杆垂类案例。
- 用真实案例帮助设计师理解适用场景。
- 引导用户跳转 Agent / Zero 实际调用。

### 3.3 共建迭代与贡献激励

让能力发布后继续成长，并让真实贡献得到记录。

产品重点：

- Issue、PR、版本与迭代摘要。
- 创建、共建、验证、反馈和维护的贡献归因。
- 活跃贡献者、新贡献者及个人贡献时间线。
- 使用问题、建议、案例和结果的结构化回流。
- 重复能力合并、过大能力拆分、失效能力归档。

### 3.4 资产与技术底座

- Wiki / Coding 代码仓库是社区内容事实源。
- `community.yaml` 提供社区展示需要的结构化字段。
- Git、PR、Issue 提供版本与贡献证据。
- 增量扫描记录“距离上次同步发生了什么变化”。
- Agent / Zero 负责采集使用上下文并生成摘要；社区只消费经过确认的结构化结果。

### 3.5 运营与治理底座

- 人工选择本次同步范围并发起扫描。
- AI 提取增量、整理信息并生成运营草稿。
- 运营人员筛选重点、修正文案、核对真实性。
- 人工确认后发布，并保存本期快照。
- 定期治理重复、失效、无人维护或需要升级的能力。

## 4. 关键闭环与系统边界

```text
个人产出
  ↓
仓库中的结构化公共能力
  ↓
社区发现、理解与贡献归因
  ↓
Agent / Zero 中真实调用
  ↓
问题、建议、案例与结果摘要
  ↓
Issue / PR / 版本更新
  ↓
仓库审核入库并再次同步社区
```

边界必须保持清楚：

- 社区不是 Agent，不记录完整对话过程。
- Agent / Zero 或外部 Agent 负责形成使用摘要、问题和建议草稿。
- 仓库负责审核并保存结构化事实。
- 社区负责集中呈现、内容运营和贡献可见。

## 5. 社区化 Input / Output

### Input：资产进入社区必须提供什么

Skill 等公共能力通过 `community.yaml` 向社区提供：

- 身份信息：ID、名称、简介、类别、版本、更新时间。
- 价值信息：解决的问题、适用场景、形成背景。
- 使用信息：使用入口、安装命令、调用范式。
- 角色信息：创建人、共建者和各自贡献。
- 演进信息：已合并贡献、版本、日期、PR / Issue 证据。
- 关联信息：相关 Wiki、规范、Skill 和知识资产。
- 可选信息：验证证据、真实案例、封面、真实使用指标。

字段示例见 `community.yaml.example`。

### Output：社区基于结构化信息呈现什么

- 公共能力的名称、价值、场景、版本与调用入口。
- 贡献者、贡献类型、贡献内容和最近贡献。
- 迭代档案、版本变化与历史摘要。
- 16.0 规范进展、社区简报和重点专题。
- 精选能力、标杆垂类案例与真实使用案例。
- 活跃贡献者、新贡献者与贡献动态。

## 6. 周度运营更新流程

推荐固定流程：

```text
01 人工发起增量同步
选择仓库、目录、资产类型和时间范围
  ↓
02 AI 提取与整理
比较上次快照，解析 community.yaml、提交和规范数量变化
  ↓
03 生成运营草稿
形成周报、16.0 进展、候选推荐、贡献动态和迭代摘要
  ↓
04 人工审核
核对来源、筛选重点、调整文案与页面顺序
  ↓
05 预览与发布
人工确认后更新社区，并保存同步快照
```

运营人员的审核点位于“AI 草稿”和“正式发布”之间。任何无法核验的数据不进入社区。

## 7. 当前官网结构

当前首页 `index.html` 的可见模块：

1. Hero：品牌、愿景、头像环和共建机制入口。
2. 16.0 设计规范 MD 进展：以周增量口径展示五类核心规范。
3. 社区贡献动态：近 30 天活跃贡献者、本周新贡献者。
4. 标杆垂类案例：当前展示商详大脑及六位共创人。
5. 本周提效应用更新：紧凑列表展示本周进入或更新于仓库的能力，并跳转线上 Coding 目录。
6. 本月精选 Skill：三项重点推荐，Relay Skill 已接入仓库结构化数据。
7. 本周精选小工具。
8. 真实使用案例。
9. 正在共创与需求广场。
10. Skill 详情抽屉、安装弹窗和共建机制弹层。

已隐藏但仍保留在 HTML 中的模块：提效工具影响力榜、设计师影响力榜。后续如果恢复，必须先接入可信指标。

辅助规划页面：

- `community-content-framework.html`：社区内容与运营框架探索。
- `community-content-structure.html`：简化的社区内容结构。
- `community-architecture.html`：早期社区架构探索稿，仅作过程参考。

## 8. 数据真实性与当前快照

### 已有真实来源或可核验来源

- `Relay 转 Design Wiki`：由仓库 `community.yaml` 与 PR / Issue 记录同步。
- 16.0 设计规范周增量：统计周期为 2026-07-08 至 2026-07-14。五类核心规范本周新增 248 份，其中场域特性 126、基础组件 49、品牌与运营 38、业务组件 29、多端多人群适配 6。详细口径见 `WEEKLY_CHANGE_CONTEXT_2026-07-08_07-15.md`。
- 社区贡献动态：Coding 主分支 2026-06-14 至 2026-07-14 的非合并提交快照，已排除 `spec-bot` 与 `skip-ci` 自动提交，页面内不是实时请求。
- 本周提效应用更新：来自本周仓库目录与内容扫描，条目链接到线上 Coding 对应文件夹；简介与进展是基于仓库内容整理后的运营文案。
- 商详大脑共创人头像：由项目方提供并放在 `assets/avatars/`。

### 当前仍为演示或人工配置

- Hero 中 `126 个 Skill / 48 位共建者 / 2,480 次有效使用`。
- 除 Relay Skill 外的大部分精选 Skill、小工具、案例和共创需求。
- 隐藏排行榜内的使用次数、节省时间和排名。
- 未接入 Agent 使用日志前，任何“有效使用次数、成功率、评分”都不能作为真实指标。
- 商详大脑简介、资产形态和团队说明目前是人工配置，不是实时仓库同步。
- Hero 头像环中的姓名与贡献说明主要用于表达社区愿景，不应作为真实贡献证据。

接手后应优先把真实数据与展示数据分层管理，禁止用演示数字替代真实指标。

## 9. 当前同步能力

同步脚本：

```bash
node scripts/sync-community-skills.js /path/to/2C-DesignWiki
```

脚本当前行为：

- 扫描仓库 `.agents/skills/*/community.yaml`。
- 解析可识别字段。
- 生成 `assets/community-skills.json`。
- 生成浏览器可直接加载的 `assets/community-skills.js`。
- 首页读取 `window.repoCommunitySkills`，把同步 Skill 注册到详情交互中。

当前限制：

- 只同步包含 `community.yaml` 的 Skill。
- YAML 解析器是项目内简易解析，不支持所有 YAML 语法。
- 不会自动调用 Coding API 拉取 PR / Issue；贡献记录需先写入 YAML，或另行接入 API 后生成。
- 不会自动发布页面；仍需运营审核后确认。
- 没有持久化“上次同步快照”的完整服务端系统。

本周提效应用更新与 16.0 周增量目前也属于“扫描后人工写入页面”的运营快照，尚未由统一脚本自动生成。

## 10. 技术结构与运行方式

项目采用原生 HTML / CSS / JavaScript，无构建依赖。

```text
index.html                         首页与弹层结构
styles.css                        视觉、响应式和动效
app.js                            页面数据、详情、弹窗与交互
server.js                         本地静态服务器
scripts/sync-community-skills.js  Skill 社区化同步脚本
assets/community-skills.json      同步后的结构化数据
assets/community-skills.js        浏览器加载版本
assets/avatars/                   头像素材
assets/hero-avatars/              Hero 头像环素材
assets/packages/                  演示安装包
```

启动方式：

```bash
cd design-skill-community-demo
node server.js
```

访问：`http://127.0.0.1:8765/index.html`

如果 8765 端口被占用：

```bash
PORT=8766 node server.js
```

## 11. 接手后的优先级

### P0：保证数据可信

- 移除或标记所有无法核验的数字。
- 固化 16.0 规范与贡献动态的统计脚本和时间窗口。
- 明确 Coding 仓库、`community.yaml`、Git / PR / Issue 的数据优先级。

### P1：把同步做成可复用流程

- 建立同步快照，稳定输出“距离上次发生的变化”。
- 增加扫描范围选择：规范、Skill、工具、独立应用。
- 将 AI 草稿、人工审核、预览发布做成运营工作台或固定 SOP。

### P2：扩展公共资产类型

- 为 Vibe Coding 工具与独立应用定义对应协议。
- 统一资产详情、贡献归因和迭代档案。
- 建立资产、规范、案例与贡献者之间的关系索引。

### P3：接入真实使用回流

- 明确 Agent / Zero 输出的使用摘要协议。
- 经用户确认后生成问题、优化建议或案例草稿。
- 审核后进入 Issue / PR / Wiki，再同步社区。

## 12. 不应改变的产品原则

1. 社区不是普通工具市场，而是组织公共能力建设系统。
2. 任何社区可见内容，都应先成为仓库或运营配置中的结构化记录。
3. 真实使用过程由 Agent / Zero 记录，社区只做最终呈现。
4. 激励不能只奖励创建，也要看见共建、验证、反馈与维护。
5. 人工运营不是临时补丁，而是当前保证质量和可信度的必要环节。
6. 不展示无法解释来源的指标，不用假头像或假贡献替代真实数据。

## 13. 视觉原则

- Apple 式极简、浅灰底、大留白、强信息层级。
- 页面未来可能嵌入已有平台，因此避免复杂主导航。
- 卡片只承载真实对象，不把每个区块都做成卡片。
- 保持黑白灰主色，蓝色作为少量强调，避免单一色调统治页面。
- 头像、版本、贡献和数据必须清晰可读，不为装饰牺牲信息真实性。

## 14. 交接结论

继续推进时，请始终围绕这条主线判断需求：

> 先把个人产出沉淀为可信的公共能力，再让它进入真实工作，通过反馈与共建持续迭代，最终形成组织级 AI 提效能力。
