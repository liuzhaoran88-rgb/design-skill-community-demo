# Claude 接手入口

这是一套“设计与用研提效社区”原型，不是普通的工具市场。它希望把个人或项目中分散的设计知识、Skill、工具和实践经验，沉淀为团队可发现、可复用、可共建、可持续演进的公共能力，并让真实贡献被看见。

## 建议先读

1. `PROJECT_HANDOFF.md`：完整背景、产品架构、系统边界与后续优先级。
2. `WEEKLY_CHANGE_CONTEXT_2026-07-08_07-15.md`：本周具体改动、真实数据口径和页面现状。
3. `community.yaml.example`：仓库内容进入社区的结构化协议。
4. `HANDOFF_CHECKLIST.md`：运行、同步和发布检查项。

## 运行当前页面

```bash
node server.js
```

浏览器打开：`http://127.0.0.1:8765/index.html`

也可以直接打开 `index.html`，但建议用本地服务检查完整交互。

## 主要修改入口

- `index.html`：首页结构和大部分静态运营内容。
- `styles.css`：视觉、响应式、动效和隐藏状态。
- `app.js`：Skill 数据、详情抽屉、安装弹窗与交互。
- `assets/community-skills.js`：浏览器加载的仓库同步 Skill 数据。
- `assets/community-skills.json`：同一份同步数据的 JSON 版本。
- `scripts/sync-community-skills.js`：从仓库 `community.yaml` 生成社区数据。

## 接手时最重要的约束

- Coding / Wiki 仓库是事实源，社区负责呈现和运营，不负责保存完整 Agent 对话。
- 任何贡献、版本、使用或评分数据都必须有可解释来源；没有来源就保留为空或标注为 Demo。
- 不要把 Hero 指标、占位卡片、隐藏排行榜数据当成正式数据。
- `Relay 转 Design Wiki`、16.0 周增量和贡献动态已有可核验快照，修改时不要随意改写口径。
- 本周提效应用列表应继续链接线上 Coding 文件夹，而不是本地路径。
- 提效工具与设计师影响力榜目前使用 `hidden` 保留，除非接入可信指标，否则不要重新展示。
- 页面可能嵌入已有平台，不增加复杂左侧导航。
- 延续当前浅灰背景、黑白主色、大留白和克制卡片的视觉语言。

## 推荐的 Claude 工作方式

1. 先说明要改哪个模块、数据来自哪里。
2. 修改前搜索现有类名和交互，沿用当前结构。
3. 页面文案优先使用仓库事实；运营性总结必须与事实字段分开。
4. 修改后同时检查桌面与移动端，尤其是长标题、贡献者和紧凑更新列表。
5. 不删除过程规划页面，它们是方案演进的参考，不是当前首页实现。

